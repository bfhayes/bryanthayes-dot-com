---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
import ProjectTile from '@/components/projects/ProjectTile.astro';

// Get all costuming projects from content collections
const allProjects = await getCollection('costuming', ({ data }) => !data.draft);
---

<BaseLayout 
  title="Costuming" 
  description="Halloween costumes, cosplay, and character design"
>
  <div class="container-custom py-12">
    <div class="text-center mb-16">
      <h1 class="text-4xl lg:text-6xl font-serif font-bold text-gray-900 mb-4">
        Costuming
      </h1>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto">
        Creating custom Halloween costumes and cosplay outfits. From character research 
        and design to pattern making, prop fabrication, and bringing ideas to life.
      </p>
    </div>

    {allProjects.length > 0 ? (
      <!-- Projects Grid -->
      <section class="mb-16">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          {allProjects.map((project) => (
            <ProjectTile project={project} category="costuming" />
          ))}
        </div>
      </section>
    ) : (
      <div class="text-center">
        <div class="w-32 h-32 mx-auto mb-8 rounded-full bg-gray-100 flex items-center justify-center">
          <span class="text-6xl opacity-60">🎭</span>
        </div>
        <p class="text-gray-600 mb-8">
          Costume portfolio and project documentation coming soon.
        </p>
        <a href="/contact" class="btn-primary">
          Discuss Custom Costumes
        </a>
      </div>
    )}

    <!-- Commission -->
    <section class="bg-gray-900 rounded-2xl p-12 text-white text-center">
      <h2 class="text-3xl font-serif font-bold mb-4 text-white">Need a Custom Costume?</h2>
      <p class="text-lg mb-8 text-gray-100">
        Let's create the perfect costume for your character or event.
      </p>
      <a href="/contact" class="btn-primary">
        Discuss Custom Costumes
      </a>
    </section>
  </div>
</BaseLayout>