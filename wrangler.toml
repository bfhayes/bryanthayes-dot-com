# Cloudflare Pages Configuration
name = "hailey-counseling"
compatibility_date = "2024-01-01"

# Build configuration
[build]
command = "npm run build:cloudflare"

# Environment variables (will be overridden by dashboard settings in production)
[vars]
DOMAIN = "haileygonnermancounseling.com"

# Note: Sensitive variables like TO_EMAIL and FROM_EMAIL should be set
# in the Cloudflare Pages dashboard, not in this file